#region Using

using System;
using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using System.IO;
using System.Linq;
using System.Reflection;
using System.Runtime.InteropServices;
using System.Text;
using String = Messages.std_msgs.String;
using Messages;
using Ros_CSharp;
using System.Threading;

#endregion


namespace Messages
{
    public static class PubSubTest
    {
        static static SetUpSub(ref List<Subscriber<IRosMessage>> subs, string myType)
        {
        $SWITCHSTATEMENT
         //SubscribeOptions <$MYTYPE>= new SubscribeOptions<$MYTYPE>(m.msgtype.ToString().Replace("__", "/").Split('/').Last(), 1, SubCall, m.MD5Sum);
         //subs.Add(node.subscribe(SubOps));
        }
    }
}